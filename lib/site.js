export const SITE_NAME = 'Track Your App';

// Placeholder until a real support inbox is set up — override via env
// rather than hardcoding a personal address.
export const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL || 'support@trackyourapp.app';
